# Cleanup summary (historical)

**Date:** 2026-04-15 (branch `expansion` at the time)

Snapshot of a prior cleanup: removed duplicate root `project_template/`, fixed test collection naming collisions, aligned telemetry with `record_provider_result`, restored Claude default model fallback, and updated routing-related tests.

For **current** large-file tracking and refactor ideas, see **[`../FILE_SIZE_FOLLOWUP.md`](../FILE_SIZE_FOLLOWUP.md)** (line counts there are maintained for follow-up work).
